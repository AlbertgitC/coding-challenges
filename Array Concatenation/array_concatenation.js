function canFormArray(arr, pieces) {
    for (let i = 0; i < arr.length; i++) {
        let curNum = arr[i];
        for (let j = 0; j < pieces.length; j++) {
            let curPiece = pieces[j];
            if (curPiece[0] === curNum) {
                
            };
        };
    };
};